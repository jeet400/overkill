<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Enhanced</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://api.fontshare.com/v2/css?f[]=general-sans@500,300,600,400,700&display=swap" rel="stylesheet">
  <style>
    .fade-in { opacity: 0; transform: translateY(20px); }
    .gradient-bg {
      background: linear-gradient(135deg, #1e1e2f 0%, #2a2a4a 50%, #1e1e2f 100%);
      background-size: 200% 200%;
      animation: gradientShift 15s ease infinite;
    }
    @keyframes gradientShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    .input-focus { transition: all 0.3s ease; }
    .input-focus:focus { transform: scale(1.02); }
    .glow-hover { transition: all 0.3s ease; }
    .glow-hover:hover { box-shadow: 0 0 20px rgba(99, 102, 241, 0.5); }
  </style>
</head>

<body class="gradient-bg text-gray-200 min-h-screen flex flex-col font-sans">
  <main class="flex-grow flex items-center justify-center p-4 sm:p-8">
    <div class="bg-white/5 p-6 sm:p-8 md:p-10 rounded-3xl shadow-xl max-w-md w-full fade-in border border-white/10 backdrop-blur-xl glow-hover">
      <div class="flex justify-center mb-6 fade-in">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-16 h-16 text-indigo-300">
          <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 1 0-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H6.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z" />
        </svg>
      </div>

      <h2 class="text-center text-4xl md:text-5xl font-bold text-white tracking-tight fade-in">Welcome Back</h2>
      <p class="text-center text-gray-400 mt-3 text-base md:text-lg fade-in">Sign in to explore your account</p>

      <!-- User/Password Login Form -->
      <form method="POST" action="/auth/login" class="mt-8 space-y-5 fade-in">
        <div>
          <label for="username" class="text-sm font-medium text-gray-300">Username</label>
          <input
            type="text"
            id="username"
            name="username"
            class="mt-1 flex-grow p-3 w-full bg-white/10 border border-white/20 focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 text-white rounded-xl placeholder-gray-500 input-focus"
            placeholder="Enter your username"
            required
          >
        </div>
        <div>
          <label for="password" class="text-sm font-medium text-gray-300">Password</label>
          <input
            type="password"
            id="password"
            name="password"
            class="mt-1 flex-grow p-3 w-full bg-white/10 border border-white/20 focus:ring-2 focus:ring-indigo-400 focus:border-indigo-400 text-white rounded-xl placeholder-gray-500 input-focus"
            placeholder="Enter your password"
            required
          >
        </div>
        <button
          type="submit"
          class="w-full flex items-center justify-center gap-2 bg-gradient-to-r from-indigo-600 to-purple-600 hover:from-indigo-700 hover:to-purple-700 text-white font-semibold py-3 px-5 rounded-xl transition duration-300 transform hover:scale-105 shadow-lg glow-hover"
        >
          <span>Sign In</span>
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5m7.5-7.5H3" />
          </svg>
        </button>
      </form>

      <!-- Register & Forgot Password -->
      <div class="text-center mt-6 text-sm fade-in">
        <p class="text-gray-400">
          Don't have an account?
          <a href="/register" class="text-indigo-300 hover:text-indigo-200 transition font-medium">Register here</a>
        </p>
        <p class="mt-3">
          <a href="/forgot-password" class="text-gray-400 hover:text-indigo-200 transition font-medium">Forgot your password?</a>
        </p>
      </div>
    </div>
  </main>

  <script>
    gsap.to(".fade-in", { opacity: 1, y: 0, duration: 0.8, stagger: 0.15, ease: "power3.out" });
  </script>
</body>
</html>
